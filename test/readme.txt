UNIT TESTS
----------

bin :
Contains HTML container and test resource for generated binaries. Binaries are not committed.

build :
Contains the .hxml files to build the tests

src : 
Contains the test classes

The tests use the uTest HaXeLib, it can be installed with the following command : 
haxelib install utest